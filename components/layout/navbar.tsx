import React from 'react'
import styles from '../../styles/components/navbar.module.scss';

type Props = {}

const Navbar = (props: Props) => {
  return (
    <div className={styles.navbar}>VACAPP</div>
  )
}

export default Navbar;