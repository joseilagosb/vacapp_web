export enum Theme {
  Light = "light",
  Dark = "dark",
}

export enum ComponentColor {
  Primary = "primary",
  Secondary = "secondary",
  Tertiary = "tertiary",
  Transparent = "transparent",
  Alert = "alert",
}
